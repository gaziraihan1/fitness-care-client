import React from 'react';
import { Link } from 'react-router';

const AllTrainer = () => {
    return (
        <div>
            <Link to={'/beATrainer'}>be a trainer</Link>
        </div>
    );
};

export default AllTrainer;