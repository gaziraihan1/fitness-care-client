import React from 'react';
import useAuth from '../../Hooks/useAuth';

const BookedTrainer = () => {
    

    return (
        <div>
            
        </div>
    );
};

export default BookedTrainer;